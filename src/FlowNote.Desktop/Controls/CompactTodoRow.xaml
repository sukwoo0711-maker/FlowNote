<UserControl x:Class="FlowNote.Desktop.Controls.CompactTodoRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="28">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="32"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Button x:Name="CompleteBox"
                Width="32" Height="28" MinHeight="28" MinWidth="32"
                Background="Transparent" BorderThickness="0" Padding="0"
                Click="OnCheck"
                AutomationProperties.Name="할 일 완료">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Grid Background="Transparent">
                        <Border Width="16" Height="16"
                                BorderBrush="#FFD1E0E7" BorderThickness="1.5"
                                CornerRadius="3" Background="Transparent"/>
                    </Grid>
                </ControlTemplate>
            </Button.Template>
        </Button>
        <Button Grid.Column="1" Background="Transparent" BorderThickness="0" Padding="0"
                HorizontalContentAlignment="Left" Click="OnOpen"
                AutomationProperties.Name="할 일 고정">
            <TextBlock Text="{Binding Title}" FontSize="12"
                       Foreground="#FFF2F6F8" TextTrimming="CharacterEllipsis"
                       VerticalAlignment="Center"/>
        </Button>
    </Grid>
</UserControl>
