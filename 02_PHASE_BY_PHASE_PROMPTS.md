# 단계별 Worker 실행 프롬프트

한 번에 아래 단계 하나만 실행한다. 전체 명세가 없는 상태에서 이 파일만으로 제품 요구를 추정하지 않는다.

## 매 단계 공통 프롬프트

```text
첨부된 전체 구현 명세를 기준으로 아래 PHASE 하나만 수행하라.
현재 코드와 docs/PROGRESS.md를 먼저 읽고 이미 완료한 작업을 보존하라.
목표에 직접 필요한 코드와 테스트만 수정하라.
구현 → 관련 테스트 → 실제 실행 가능한 범위 확인 → 오류 수정 순서로 진행하라.
결과를 docs/PROGRESS.md와 docs/TEST_RESULTS.md에 남겨라.
실행하지 못한 검수는 NOT RUN 또는 BLOCKED로 표시하라.
이번 단계의 실패가 해결되기 전에는 다음 단계로 넘어가지 마라.
보고: 수행 단계 / 변경 파일 / 실제 명령과 결과 / 증거 경로 / 미검증 / 다음 한 단계.
```

테스트 ID는 `03_ACCEPTANCE_TESTS.md`에 대응한다. 단계 초반에는 해당 단계에서 구현한 범위만 확인하고, 아직 후속 화면이 필요한 통합 테스트 전체를 PASS 처리하지 않는다. 부분 결과는 따로 기록하며 전체 검수는 PHASE 12~13에서 완결한다. 파일 변경 개수는 억지로 제한하지 않지만 unrelated 리팩터링은 하지 않는다.

---

## PHASE 00 — 환경·범위·기준선 확인

**참조 명세:** 0~3, 14~15.

**이번 실행 지시**

현재 저장소/운영체제/SDK/Windows GUI 실행 가능 여부를 조사한다. 기존 코드가 있으면 구조와 현재 빌드 결과를 먼저 남긴다. Windows 우선·기술 스택·시간 측정·암호화의 확정/미확정을 DECISIONS.md에 구분한다. 새 프로젝트이고 Windows 스택이 확정되었다면 최소 솔루션과 Core 테스트 1개, 실행 가능한 빈 WPF 창을 만든다. 명세를 docs/SPEC.md로 보존하고 진행·검수 문서를 만든다.

**하지 말 것**

데이터 모델 전체, 화려한 UI, 외부 패키지 대량 설치를 아직 하지 않는다. 기존 코드를 지우지 않는다.

**단계 통과 조건**

현재 빌드·테스트 명령 결과와 Windows UI 검증 가능 여부가 명시되어 있어야 한다. 미확정 스택 상태에서는 그 결정을 확정했다고 주장하지 않는다.

**검수 연결:** A01.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 01 — 저장 기반과 시간 모델

**참조 명세:** 7~9, 15.

**이번 실행 지시**

Core 모델, IClock, 표시 시간대 서비스, 초기 SQL migration, DatabaseExecutor, Entry 저장/조회 서비스를 만든다. 사용자 데이터 경로와 데모 데이터 경로를 분리한다. request_id 중복 방지와 시각 정렬을 테스트한다. 직접 입력값을 SQL 문자열에 붙이지 않는다. migration 실패 시 원래 DB를 보존한다.

**하지 말 것**

타임라인 화면, AI, Jira 호출을 구현하지 않는다. 테스트용 고정 시계를 일반 모드에 연결하지 않는다.

**단계 통과 조건**

텍스트 Entry 저장→연결 종료→재조회가 실제 임시 DB에서 통과해야 한다. UTC/한국 날짜 경계/동일 시각 seq 정렬/중복 저장 방지를 검증한다.

**검수 연결:** A09–A14, A71, A74.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 02 — 플로팅 텍스트 기록의 첫 완주

**참조 명세:** F01, 8.3, 9.3, 12.

**이번 실행 지시**

단순한 FloatingCapsuleWindow를 만들고 한 줄 입력→저장→로컬 DB를 연결한다. **SUPERSEDED BY docs/FLOATING_CAPSULE_V2.md:** Enter 줄바꿈/세로 FloatingDockWindow 지시는 폐기.

**하지 말 것**

디자인 마감이나 다른 타임라인 보기부터 만들지 않는다. 저장 실패를 토스트만 띄우고 입력을 비우지 않는다.

**단계 통과 조건**

한글 텍스트를 저장하고 앱 종료/재실행 후 복구한다. 저장된 Entry와 초안을 구별한다. 실제 Windows IME를 실행하지 못했으면 해당 검수는 NOT RUN이다.

**검수 연결:** A04–A08, A56, A58.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 03 — TODO 완료가 이력이 되는 규칙

**참조 명세:** W01, 8 전체.

**이번 실행 지시**

WorkItemService의 생성/완료/재열기/취소와 이벤트 저장을 구현한다. 트랜잭션·expectedVersion·requestId로 이중 완료를 막는다. F01 하단 활성 TODO를 표시하고 체크 성공 뒤 숨긴다. 실행 취소는 재열기 Entry다. 여러 열린 창의 상태 알림을 연결한다. 날짜별 완료 집계를 이력 기준으로 계산한다.

**하지 말 것**

완료를 DELETE로 구현하지 않는다. 현재 상태만으로 과거 날짜의 완료 수를 계산하지 않는다.

**단계 통과 조건**

완료와 이벤트 저장의 원자성, 두 번 클릭, 두 창의 동시 완료, 재열기와 다음 날 재열기 집계를 검증한다.

**검수 연결:** A15–A24, A33.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 04 — 이미지·파일 첨부 파이프라인

**참조 명세:** D01 첨부, 7, 9.4~9.5.

**이번 실행 지시**

사용자 Ctrl+V/파일 선택/드래그앤드롭으로 첨부를 받는다. staging→사본 복사→안전한 파일명→메타데이터/해시→DB 관계→커밋 후 UI 반영 순서를 만든다. 원본과 썸네일을 구별한다. 이미지 단독 기록, 다중 첨부, 제한 초과, 깨진 이미지, 복사/커밋 실패를 처리한다. 초안 첨부도 재실행 후 복구한다.

**하지 말 것**

클립보드 상시 감시, OCR, 캡처 자동화를 만들지 않는다. 파일 경로만 저장하고 원본에 의존하지 않는다.

**단계 통과 조건**

원본을 옮겨도 앱 사본이 열린다. 파일 복사·DB 커밋 중 실패해도 반쪽 성공으로 표시하지 않는다.

**검수 연결:** A25–A32, A43.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 05 — 데스크톱 디자인 토큰과 메인 구조

**참조 명세:** 1, 4~5, O01, T01 상단.

**이번 실행 지시**

참고 이미지에서 색·노드·카드·첨부 표현만 추출한다. 공통 ResourceDictionary와 재사용 컨트롤을 만든다. 좌측 탐색/중앙/우측 상세 공간을 가진 실제 메인창을 구현한다. 폭에 따른 상세 오버레이와 축소 탐색을 만든다. 첫 실행 안내와 별도 데모 모드를 연결한다.

**하지 말 것**

휴대전화 프레임, 마케팅 포스터, 모바일 하단 탭, 미지원 플랫폼 로고를 넣지 않는다. 실제 기록 UI를 이미지 한 장으로 대체하지 않는다.

**단계 통과 조건**

실제 DB를 사용한 메인창과 F01 캡처를 제출한다. 빈 상태도 있다. 창을 줄여도 입력/주요 버튼이 잘리지 않는다.

**검수 연결:** A02–A03, A62, A67, A69.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 06 — 시간순 하루 타임라인 완성

**참조 명세:** T01, 7 날짜 투영, 8.4.

**이번 실행 지시**

선택 날짜의 Entry를 시간순으로 조회해 시각 열/노드/카드/생명주기 행으로 렌더링한다. 날짜 이동·오늘·프로젝트/업무/유형 필터·헤더 집계를 실제 데이터에 연결한다. 이미지 썸네일과 상태 표기를 넣는다. 실시간 갱신 때 사용자의 스크롤과 과거 날짜를 유지한다. 목록 가상화와 범위 조회를 적용한다.

**하지 말 것**

기록이 없는 간격을 휴식으로 표현하지 않는다. 근무시간 숫자를 만들지 않는다. 모든 항목을 새로 생성해 스크롤을 맨 위로 돌리지 않는다.

**단계 통과 조건**

빈 날/한 기록/밀집 기록/자정 경계/장문/첨부 혼합/스크롤 중 새 기록을 각각 확인한다.

**검수 연결:** A10–A14, A33–A35, A68.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 07 — 근거 상세와 변경 이력

**참조 명세:** D01, 7 revision, 9.5, 10 삭제.

**이번 실행 지시**

D01 패널에 원문, 두 종류 시각, 업무/프로젝트/이슈, 첨부, 관련 기록을 표시한다. 일반 메모 편집 시 revision을 저장한다. 이미지 확대를 만든다. 지원하지 않는 파일은 정직한 대체 표시를 사용한다. 누락 파일·삭제·복원 상태를 준비한다. 보이는 항목과 명령에 접근성 이름을 제공한다.

**하지 말 것**

생명주기 이벤트의 시각·본문을 자유 편집하지 않는다. 외부 파일/링크를 자동으로 열지 않는다.

**단계 통과 조건**

편집 전 원문·시각·첨부 연결을 확인할 수 있다. 메모 내용만 남고 첨부가 누락된 경우도 앱이 정상 사용 가능해야 한다.

**검수 연결:** A10, A13, A23, A42–A44, A72.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 08 — 하루 파노라마 핵심 화면

**참조 명세:** T02 전체, 7 날짜 투영, 12 성능.

**이번 실행 지시**

00~24시 미니맵과 가로 스크롤 24시간 열을 구현한다. 실제 시각을 유지하며 15분 밀집 클러스터와 시간 열별 3개+더보기를 연결한다. 미니맵 클릭→확대 영역 이동→기록 선택→D01→T01 동일 기록을 연결한다. 날짜/필터/선택 상태는 공통 상태를 쓴다.

**하지 말 것**

소요시간 막대, 임의 업무시간, 장식용 가짜 카드, 내용을 숨겨버리는 잘림을 만들지 않는다.

**단계 통과 조건**

09:42 기록이 두 보기에서 같은 ID/내용/시각으로 열리고 밀집된 20개 기록 모두 접근 가능하다. 미니맵은 하루 전체를 포함한다.

**검수 연결:** A34, A36–A38, A69.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 09 — 업무별 보기·전체 TODO·검색

**참조 명세:** T03, W01, Q01.

**이번 실행 지시**

같은 날짜의 기록을 WorkItem별로 묶고 업무 미연결 그룹을 유지한다. 해당 날짜와 업무 전체 기록을 구별한다. W01 진행/완료/취소 관리와 제목 변경을 연결한다. 한국어 부분 문자열 검색, 검색 디바운스·오래된 결과 폐기·날짜 원위치 이동을 구현한다.

**하지 말 것**

자동 업무 분류, 벡터 검색, 복잡한 프로젝트 관리 기능을 추가하지 않는다.

**단계 통과 조건**

검색 결과가 해당 날짜의 같은 Entry를 연다. 업무명 변경은 과거 이벤트 스냅샷을 바꾸지 않는다.

**검수 연결:** A23–A24, A39–A41, A45.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 10 — 선택 보고서와 수동 이슈 연결

**참조 명세:** R01, 11.

**이번 실행 지시**

이슈 번호/HTTP·HTTPS URL 수동 저장을 연결한다. URL 스킴을 검증하고 클릭 때만 브라우저에 넘긴다. 하루 정리의 선택/첨부 OFF 기본/미리보기/복사/Markdown ZIP 저장을 구현한다. 상대 경로 첨부 링크와 manifest를 생성한다. 선택되지 않은 민감 기록을 포함하지 않는다.

**하지 말 것**

Jira API, 링크 제목 자동 조회, 가짜 동기화 완료, AI 요약을 구현하지 않는다.

**단계 통과 조건**

2개 중 1개 선택 내보내기에서 미선택 기록·첨부가 없고 다른 경로에 풀어도 링크가 유효하다.

**검수 연결:** A44, A46–A48, A65–A66.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 11 — 백업·복원·휴지통·설정

**참조 명세:** C01, 10 전체.

**이번 실행 지시**

일관된 DB 스냅샷+참조 첨부+manifest의 수동 백업을 구현한다. 복원은 staging 검증→현재 안전 사본→앱 종료→다음 시작 적용 순서로 구현한다. 경로 탈출·압축 폭탄·손상·버전 불일치·교체 중단을 다룬다. 일반 메모 휴지통/복원/영구 삭제와 참조 중 파일 보존을 구현한다.

**하지 말 것**

실행 중 DB 단순 복사, 검증 전 덮어쓰기, 휴지통 자동 영구 삭제, 보안 모드라는 과장된 이름을 쓰지 않는다.

**단계 통과 조건**

백업에서 실제 복원하고 내용·시각·첨부 해시가 일치한다. 중단 시 원본/복원본 중 유효한 상태로 복구한다.

**검수 연결:** A49–A55, A73–A74.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 12 — Windows 실제 환경과 회귀 검수

**참조 명세:** 12 전체, 14~15.

**이번 실행 지시**

단일 인스턴스, 전역 단축키 충돌, 핀과 포커스 분리, 트레이 종료, 모니터 탈착, DPI, 절전/자정, 모달 소유자, 로그인 시 실행 OFF 기본을 실제 검수한다. 오프라인 작업과 외부 요청 부재를 확인한다. 10,000개 기록/1,000개 첨부 fixture로 성능을 측정한다. 깨진 부분만 고친다.

**하지 말 것**

새 기능 추가, 프레임워크 교체, GUI 없는 환경에서의 허위 PASS를 금지한다.

**단계 통과 조건**

검수표 모든 항목에 실제 상태와 근거를 기입한다. NOT RUN이 있으면 최종 검수 완료라고 하지 않는다.

**검수 연결:** A56–A68, A74–A76 및 전체 회귀.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.

---

## PHASE 13 — 실행 패키지와 인수인계

**참조 명세:** 13, 15.

**이번 실행 지시**

실제 restore/build/test/publish를 실행해 베타 실행 ZIP과 소스·스크립트·잠금파일·라이선스·사용안내·백업/복원안내·검수결과·실제 스크린샷을 묶는다. 개발 SDK가 없는 허용된 깨끗한 Windows 환경에서 패키지 시작과 핵심 흐름을 확인한다. 공개 유료 배포 미포함 항목을 별도로 적는다.

**하지 말 것**

프로토타입 이미지를 프로그램 캡처라고 제출하거나 베타를 판매 준비 완료라고 주장하지 않는다.

**단계 통과 조건**

인수자가 실행 파일로 기록→완료→타임라인→내보내기→재시작을 재현하고, 소스에서 다시 빌드할 수 있다.

**검수 연결:** A01, A69–A70 및 최종 회귀.

통과 또는 미검증 사유를 기록하고 이번 실행을 마친다. 다음 단계 전체를 함께 구현하지 않는다.
